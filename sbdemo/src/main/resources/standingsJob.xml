<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       	http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/batch
		http://www.springframework.org/schema/batch/spring-batch-2.1.xsd
       	http://www.springframework.org/schema/aop
 		http://www.springframework.org/schema/aop/spring-aop.xsd">

    <import resource="applicationContext.xml"/>

    <job id="standingsJob" restartable="false" xmlns="http://www.springframework.org/schema/batch">
        <step id="standingStep1">
            <tasklet>
                <chunk reader="dbGameReader" processor="standingsProcessor" writer="standingWriter" commit-interval="1"/>
            </tasklet>
        </step>

    </job>

    <!-- Read games via hHibernate -->
    <bean id="dbGameReader" class="org.springframework.batch.item.database.HibernateCursorItemReader">
        <property name="sessionFactory" ref="sessionFactory" />
        <property name="queryString" value="from Game" />
    </bean>

    <!-- Update standing for each game -->
    <bean id="standingsProcessor" class="com.objectpartners.sbdemo.standingssjob.StandingProcessor">
        <property name="teamService" ref="teamService" />
    </bean>

    <!-- Write new standings -->
    <bean id="standingWriter" class="com.objectpartners.sbdemo.standingssjob.StandingWriter">
        <property name="teamService" ref="teamService" />
    </bean>
</beans>